"""Ingestion pipeline (document_loader, parser, cleaner, chunker,
metadata_extractor, embedding_service, vector_store, indexing_service),
retrieval (dense/sparse/hybrid + reranking), and the LLM provider
abstraction (`app/rag/llm`). Populated starting Phase 2.
"""
